<aside>
    <ul>
        <li>
            <span>Archief</span>
            <ul class="second">
                {% for year, itemMonths in news %}
                    <li><span>{{ year }}</span></li>
                    <ul class="second">
                        {% for month, items in itemMonths %}
                            <li><a href="{{ path('monthNews', {year: year, month: month }) }}">{{ month|capitalize }}</a></li>
                        {% endfor %}
                    </ul>
                {% endfor %}
            </ul>
        </li>
    </ul>
</aside>